# SchoolDash Frontend

React + Vite frontend for the SchoolDash dashboard:

- Admin login with simple auth flow
- Dashboard with enrollment, course distribution, and attendance charts
- Weather widget using the OpenWeather API

This app is designed to work with the Laravel backend in `Finals_BackEnd`.

